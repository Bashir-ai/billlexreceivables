{
  "buildCommand": "npx prisma generate && npx prisma db push --accept-data-loss && npx next build && node scripts/copy-pdfkit-fonts.js",
  "crons": [
    {
      "path": "/api/cron/check-outstanding-invoices",
      "schedule": "0 9 * * *"
    }
  ]
}
